<?php

namespace triboon\pubjet\includes\enums;

// Exit if accessed directly
defined('ABSPATH') || exit;

abstract class EnumTransients {
    const PublishFutureBacklinks   = 'pubjet_publish_future_backlinks';
}